---
apiVersion: batch/v1
kind: Job
metadata:
  name: inject-admin-user
  namespace: prod
spec:
  ttlSecondsAfterFinished: 300
  template:
    spec:
      containers:
      - name: injector
        image: skyacrprodel5zpw.azurecr.io/sky-poc-backend:latest
        command: ["python", "-c"]
        args:
        - |
          import asyncio
          import os
          from sqlalchemy.ext.asyncio import create_async_engine, AsyncSession
          from sqlalchemy.orm import sessionmaker
          from sqlalchemy.future import select
          from src.models.user import User
          from passlib.context import CryptContext

          async def run():
              db_url = os.getenv("DATABASE_URL")
              # Ensure driver
              db_url = db_url.replace("postgresql://", "postgresql+asyncpg://")

              print(f"Connecting to DB...")
              engine = create_async_engine(db_url)
              async_session = sessionmaker(engine, class_=AsyncSession, expire_on_commit=False)

              ctx = CryptContext(schemes=["bcrypt"], deprecated="auto")

              async with async_session() as session:
                  email = "admin@skyfirstlabs.com"
                  stmt = select(User).where(User.email == email)
                  result = await session.execute(stmt)
                  existing = result.scalars().first()

                  if existing:
                      print(f"User {email} already exists. Updating password/role...")
                      existing.password_hash = ctx.hash("SkyFirst@2026!")
                      existing.role = "admin"
                      await session.commit()
                      print("✅ User updated.")
                  else:
                      print(f"Creating new admin...")
                      user = User(
                          email=email,
                          password_hash=ctx.hash("SkyFirst@2026!"),
                          name="Admin User",
                          role="admin",
                          auth_provider="local",
                          email_verified=True,
                          has_completed_onboarding=True
                      )
                      session.add(user)
                      await session.commit()
                      print(f"✅ User created: {email}")

          if __name__ == "__main__":
              asyncio.run(run())
        env:
        # Recuperar secrets do segredo montado pelo ExternalSecret
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: sky-be-prd-common-app-secrets
              key: database_url
      restartPolicy: Never
