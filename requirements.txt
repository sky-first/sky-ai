# --- FastAPI + Uvicorn ---
fastapi==0.110.0
uvicorn[standard]==0.29.0

# --- LangGraph + LangChain Core + OpenAI ---
langgraph==0.2.19
langgraph-checkpoint-postgres==1.0.3  # Conversational memory
langchain-core>=0.3.15,<0.4.0
langchain-openai==0.2.5
langchain-community>=0.0.20
langchain-ollama>=0.2.0
# AWS Bedrock provider — enabled when AI_PROVIDER=bedrock. Pulls boto3
# transitively; on EKS it picks up credentials via IRSA.
langchain-aws>=0.2.4,<0.3.0

# --- YAML ---
pyyaml==6.0.1

# --- BigQuery + Google Auth ---
google-cloud-bigquery==3.25.0
google-cloud-bigquery-storage>=2.20.0
google-auth==2.29.0
google-auth-oauthlib==1.2.0

# --- Utils ---
python-dotenv==1.0.1
pydantic>=2.7.4,<3.0.0
pydantic-settings>=2.3.4,<3.0.0

# --- Language detection ---
lingua-language-detector==2.1.1

SQLAlchemy==2.0.31
psycopg[binary]==3.2.13
pgvector==0.3.4
psycopg2-binary==2.9.10
asyncpg>=0.29.0
greenlet>=3.0.0
alembic>=1.13.0,<2.0.0

# --- SQL Parsing (Advanced Validator) ---
sqlparse>=0.4.4

# Additional dependencies
greenlet==3.1.1

# --- Redis (Distributed Rate Limiting + Progressive Escalation) ---
redis>=5.0.0

# --- Celery (Background tasks) ---
celery>=5.3.0

# --- Data Processing (DuckDB Engine) ---
pandas>=2.2.0
duckdb>=0.10.0
pyarrow>=15.0.0
prometheus-fastapi-instrumentator>=6.0.0
PyJWT==2.8.0

# --- Connection config decryption (Fernet, shared with backend) ---
cryptography>=42.0.0

# --- Semantic projection (Universe Intelligence v2 canvas) ---
# UMAP reduces 768-dim Nomic embeddings to 2D/3D for the constellation
# canvas; HDBSCAN clusters the projected coords so the FE can render
# centroids at zoom-out vs. individuals at zoom-in. scikit-learn is a
# transitive of both but pinned here to lock the version that umap-learn
# resolves against.
umap-learn>=0.5.5
scikit-learn>=1.4.0
hdbscan>=0.8.33
sqlglot>=30,<31

# --- Embeddings locais (EMBEDDING_PROVIDER=local) ---
# ONNX runtime, NÃO torch. sentence-transformers arrastaria ~2GB de torch
# para a imagem; o fastembed faz o mesmo trabalho com modelos quantizados
# e uma biblioteca de ~50MB.
#
# Existe porque a inferência on-demand do Bedrock está bloqueada ao nível
# da conta AWS (quota 0 e não ajustável em eu-west-1; estrangulada também
# em us-east-1 apesar da quota dizer 6000), e o proxy mantle que salva o
# chat serve 35 modelos de chat e zero de embedding. Correr o modelo
# dentro do próprio pod do sky-ai mantém o gasto nos créditos AWS sem
# acrescentar pods nem nós.
fastembed>=0.4.0,<0.8.0
