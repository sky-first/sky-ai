apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: sky-fe-teamblue-stg
  namespace: argocd
  annotations:
    # Escreve de volta em arquivo EXCLUSIVO do TeamBlue - sem afetar o Staging
    argocd-image-updater.argoproj.io/image-list: sky-poc-frontend=skyacrstagingj3minh.azurecr.io/sky-poc-frontend:^sha-.*
    argocd-image-updater.argoproj.io/sky-poc-frontend.update-strategy: latest
    argocd-image-updater.argoproj.io/write-back-method: git:secret:argocd/git-creds
    argocd-image-updater.argoproj.io/write-back-target: branch:staging://gitops/charts/common-app/values-teamblue-fe-stg.yaml
spec:
  project: default
  source:
    repoURL: "https://github.com/sky-first/sky-poc-infra.git"
    path: gitops/charts/common-app
    targetRevision: staging
    helm:
      valueFiles:
        - values-teamblue-fe-stg.yaml
      values: |
        imagePullSecrets:
          - name: regcred
        image:
          repository: skyacrstagingj3minh.azurecr.io/sky-poc-frontend
        service:
          port: 80
          targetPort: 3000
        ingress:
          enabled: true
          className: "nginx"
          annotations:
            cert-manager.io/cluster-issuer: letsencrypt-prod
            nginx.ingress.kubernetes.io/modsecurity-snippet: "SecRuleRemoveById 942100"
            nginx.ingress.kubernetes.io/proxy-set-header: "Accept-Encoding ''"
            nginx.ingress.kubernetes.io/configuration-snippet: |
              proxy_set_header Accept-Encoding "";
              sub_filter '<title>Sky</title>' '<title>TeamBlue</title>';
              sub_filter '<title>SKY</title>' '<title>TeamBlue</title>';
              sub_filter '\\\"children\\\":\\\"SKY\\\"' '\\\"children\\\":\\\"TeamBlue\\\"';
              sub_filter '\\\"children\\\":\\\"Sky\\\"' '\\\"children\\\":\\\"TeamBlue\\\"';
              sub_filter '\"children\":\"SKY\"' '\"children\":\"TeamBlue\"';
              sub_filter '">SKY<' '">TeamBlue<';
              sub_filter '"SKY"' '"TeamBlue"';
              sub_filter '/icon.png' 'https://skybkpstagingol4bzp.blob.core.windows.net/public-assets/teamblue-favicon.png?v=1';
              sub_filter '/favicon.ico' 'https://skybkpstagingol4bzp.blob.core.windows.net/public-assets/teamblue-favicon.png?v=1';
              sub_filter_once off;
              sub_filter_types text/html application/json;
          hosts:
            - host: teamblue.skyfirstlabs.com
              paths: &ingress_paths
                - path: /
                  pathType: Prefix
            - host: plataform.teamblue-stg.skyfirstlabs.com
              paths: *ingress_paths
          tls:
            - secretName: sky-fe-teamblue-tls
              hosts:
                - teamblue.skyfirstlabs.com
                - plataform.teamblue-stg.skyfirstlabs.com
        resources:
          requests:
            cpu: 100m
            memory: 256Mi
          limits:
            cpu: 100m
            memory: 256Mi
        probes:
          liveness:
            path: /
            initialDelaySeconds: 15
          readiness:
            path: /
            initialDelaySeconds: 5
        securityContext:
          runAsNonRoot: true
          runAsUser: 1000
          runAsGroup: 3000
          fsGroup: 2000
          readOnlyRootFilesystem: true
          allowPrivilegeEscalation: false
          capabilities:
            drop:
              - ALL
        autoscaling:
          enabled: true
          minReplicas: 2
          maxReplicas: 10
        externalSecret:
          enabled: true
          secretStoreRef:
            name: azure-keyvault
            kind: ClusterSecretStore
          target:
            creationPolicy: Owner
            template:
              engineVersion: v2
              data:
                NEXTAUTH_SECRET: "{{ .nextauth_secret }}"
          data:
            - secretKey: nextauth_secret
              remoteRef:
                key: nextauth-secret
        envSecrets:
          NEXTAUTH_SECRET:
            name: sky-fe-teamblue-stg-common-app-secrets
            key: NEXTAUTH_SECRET
        extraEnv:
          - name: NEXT_PUBLIC_API_URL
            value: "https://plataform.teamblue-stg.skyfirstlabs.com"
          - name: NEXTAUTH_URL
            value: "https://plataform.teamblue-stg.skyfirstlabs.com"
  destination:
    server: "https://kubernetes.default.svc"
    namespace: teamblue
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
