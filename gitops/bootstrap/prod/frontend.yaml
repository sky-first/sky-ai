apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: sky-fe-prd
  namespace: argocd
spec:
  project: default
  source:
    repoURL: "https://github.com/sky-first/sky-poc-infra.git"
    path: gitops/charts/common-app
    targetRevision: main
    helm:
      values: |
        imagePullSecrets:
          - name: regcred
        image:
          repository: skyacrprodel5zpw.azurecr.io/sky-poc-frontend
          tag: "latest"
        
        securityContext:
          runAsNonRoot: true
          runAsUser: 1000
          runAsGroup: 3000
          fsGroup: 2000
          readOnlyRootFilesystem: true
          allowPrivilegeEscalation: false
          capabilities:
            drop:
              - ALL
            add:
              - NET_BIND_SERVICE
        
        service:
          port: 80
          targetPort: 3000
        
        ingress:
          enabled: true
          className: "nginx"
          annotations:
            nginx.ingress.kubernetes.io/whitelist-source-range: "81.84.211.111/32, 20.97.244.91/32, 169.155.237.158/32"
            cert-manager.io/cluster-issuer: "letsencrypt-prod"
          hosts:
            - host: workspace-prd.skyfirstlabs.com
              paths:
                - path: /
                  pathType: Prefix
          tls:
            - secretName: sky-fe-prd-tls
              hosts:
                - workspace-prd.skyfirstlabs.com
        autoscaling:
          enabled: true
          minReplicas: 2
          maxReplicas: 10
          targetCPUUtilizationPercentage: 70
          targetMemoryUtilizationPercentage: 80
        
        resources:
          requests:
            cpu: 200m
            memory: 384Mi
          limits:
            cpu: 1000m
            memory: 768Mi
        
        extraEnv:
          - name: NEXT_PUBLIC_API_URL
            value: "https://workspace-prd.skyfirstlabs.com/api"
          - name: ROLLOUT_TRIGGER
            value: "2026-02-02T09:52:00Z"
  destination:
    server: "https://kubernetes.default.svc"
    namespace: prod
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
