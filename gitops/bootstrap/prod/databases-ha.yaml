apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: sky-postgresql-ha-prod
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://charts.bitnami.com/bitnami
    chart: postgresql
    targetRevision: 12.1.8
    helm:
      releaseName: postgresql
      values: |
        global:
          imageRegistry: skyacrprodel5zpw.azurecr.io
          imagePullSecrets:
            - regcred
          postgresql:
            auth:
              postgresPassword: "qL2fmcDPM02ec3yj"
              username: sky
              password: "qL2fmcDPM02ec3yj"
              database: ai_saas_db
        
        image:
           registry: skyacrprodel5zpw.azurecr.io
           repository: bitnami/postgresql
           tag: latest

        # High Availability configuration
        postgresql:
          replication:
            enabled: true
            user: replicator
            password: "qL2fmcDPM02ec3yj"
            readOnlyUser:
              name: sky_readonly
              password: "qL2fmcDPM02ec3yj"
          
          # Streaming replication setup
          maxConnections: 200
          sharedBuffers: 256MB
        
        primary:
          persistence:
            enabled: true
            size: 100Gi
            storageClassName: premium-rwo
        
        readReplicas:
          enabled: true
          replicaCount: 2
          persistence:
            enabled: true
            size: 100Gi
            storageClassName: premium-rwo
        
        # Backup configuration
        backup:
          enabled: true
          cronjob:
            schedule: "0 2 * * *"  # Daily at 2 AM
            storage:
              existingClaim: postgresql-backup-pvc
        
        metrics:
          enabled: true
          image:
            registry: skyacrprodel5zpw.azurecr.io
            repository: bitnami/postgres-exporter
            tag: latest
          serviceMonitor:
            enabled: true
            labels:
              prometheus: prod
  
  destination:
    server: https://kubernetes.default.svc
    namespace: prod
  
  syncPolicy:
    automated:
      prune: false  # Never auto-delete DB pods
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
    retry:
      limit: 5

---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: sky-redis-ha-prod
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://charts.bitnami.com/bitnami
    chart: redis
    targetRevision: 17.10.3
    helm:
      releaseName: redis
      values: |
        global:
          imagePullSecrets:
            - regcred
        
        image:
          registry: skyacrprodel5zpw.azurecr.io
          repository: bitnami/redis
          tag: latest
        
        auth:
          enabled: true
          password: "LDVUBr9jYhxnddxhvbDpDliO"
        
        # Architecture standalone to avoid write errors in workers
        architecture: standalone
        sentinel:
          enabled: false
        
        replica:
          replicaCount: 0
          persistence:
            enabled: true
            size: 10Gi
            storageClassName: premium-rwo
        
        master:
          persistence:
            enabled: true
            size: 10Gi
            storageClassName: premium-rwo
        
        # Resource limits
        resources:
          requests:
            cpu: 100m
            memory: 256Mi
          limits:
            cpu: 500m
            memory: 512Mi
        
        metrics:
          enabled: true
          image:
            registry: skyacrprodel5zpw.azurecr.io
            repository: bitnami/redis-exporter
            tag: latest
          serviceMonitor:
            enabled: true
            labels:
              prometheus: prod
  
  destination:
    server: https://kubernetes.default.svc
    namespace: prod
  
  syncPolicy:
    automated:
      prune: false  # Never auto-delete cache pods
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
    retry:
      limit: 5

---
# PVC for PostgreSQL backups
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgresql-backup-pvc
  namespace: prod
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: premium-rwo
  resources:
    requests:
      storage: 100Gi
