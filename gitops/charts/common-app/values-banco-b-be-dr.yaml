# DR values — imagem fixada (image-updater não roda no cluster DR)
# Tag atualizada manualmente durante onboarding ou via script de sync
image:
  repository: "BANCO_B_ACR.azurecr.io/sky-poc-backend"
  tag: "sha-latest"
  pullPolicy: IfNotPresent
